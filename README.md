# جديد — نموذج تفاعلي (React + Tailwind CSS)

نموذج MVP تفاعلي كامل لتطبيق «جديد» (متسوّق + تاجر) مبني على تصميم Figma
`Jadeed UI`، مع تصحيح أخطاء التصميم وتطبيق منطق حقيقي للسلة والطلبات.

## التشغيل

```bash
npm install
npm run dev        # http://localhost:5173
npm run build      # الإخراج في dist/
npm run preview    # معاينة نسخة الإنتاج
```

## نقطة الدخول (Landing للمطوّر / المُقيّم)

عند فتح التطبيق تظهر شاشة ترحيبية بخيارين:

| الخيار | الرابط | الوصف |
|---|---|---|
| **معرض الشاشات** | `#/gallery` أو `#/gallery/<key>` | 87 شاشة وحالة مصنّفة في 9 فئات (التهيئة، التسجيل، الموقع، العميل، السلة والطلبات، الحساب، توثيق التاجر، لوحة التاجر، المكونات المستقلة). كل شاشة تُعرض معزولة بحالتها الخاصة (`src/gallery/catalog.js`) مع بحث بالاسم/الرمز، تنقّل بالأسهم ← →، وزر إعادة ضبط. |
| **تجربة النموذج الأولي** | `#/prototype` | التطبيق التفاعلي الكامل من شاشة البداية (Splash → التهيئة → نوع الحساب → الدخول/OTP → التسوق → السلة → الطلب → التتبع) مع لوحة عرض جانبية واختصارات. |

## بيانات العرض التجريبي

| العنصر | القيمة |
|---|---|
| تسجيل الدخول | أي بريد صالح أو رقم يبدأ بـ `7` من 9 أرقام (مثال `773030064`) |
| رمز التحقق OTP | `1234` (4 أرقام — 3 محاولات خاطئة ⇒ قفل مؤقت CUS-006) |
| الخصومات والكوبونات | موقوفة مؤقتاً بقرار المنتج (RC-7) — لا حقل كوبون ولا أسعار قديمة/شارات خصم |
| رمز OTP منتهي الصلاحية | العدّاد 60 ثانية داخل الشاشة؛ حالة الانتهاء الجاهزة في المعرض `otp-expired` |
| انقطاع الإنترنت | زر «محاكاة: انقطاع الإنترنت» في لوحة العرض (أو فصل الشبكة فعلياً — يُقرأ `navigator.onLine`) |
| محاكاة فشل التسجيل | بريد يحتوي على كلمة `fail` |
| أرقام الطلبات | صيغة موحّدة `JD-XXXXXX` (أول طلب جديد `JD-984210`) |

على الشاشات العريضة تظهر **لوحة العرض (Demo Panel)** يسار الهاتف وفيها اختصارات
(إعادة التشغيل، الدخول كعميل، لوحة التاجر، محاكاة متجر محظور، شاشة الدخول، رحلة توثيق تاجر، محاكاة انقطاع الإنترنت) وقراءة حيّة للحالة.

## ما تم تصحيحه مقارنةً بالتصميم

- **Design Tokens موحّدة** في `tailwind.config.js`: `primary #5002C9`، `secondary #FF5715`،
  تدرّج رمادي واحد `ink 50–900`، ألوان الحالات، نصف الأقطار، الظلال.
- **OTP = 4 أرقام** في كل الشاشات (الرسالة، الحقول، القفل).
- **أرقام الطلبات** بصيغة واحدة `JD-XXXXXX` في السلة/التتبع/الفاتورة/لوحة التاجر.
- **شريط تنقّل سفلي موحّد** (ترتيب وأيقونات ثابتة، RTL) للعميل وللتاجر.
- **حسابات السلة رياضية حقيقية**: المجموع الفرعي + التوصيل = الإجمالي (لا أرقام ثابتة؛ الخصومات موقوفة مؤقتاً).
- تصحيح الأخطاء الإملائية والمنطقية في النصوص.

## المنطق التفاعلي (Context API + useReducer)

- إضافة للسلة تزيد العدّاد فعلياً (بحدّ المخزون الحقيقي لكل منتج)، تعديل الكميات، حذف، إتمام الطلب مع بيانات التواصل والاستلام.
- دورة حياة الطلب: جديد → مقبول → قيد التحضير → جاهز → خرج للتوصيل → تم التوصيل
  (تتقدّم تلقائياً أو يدوياً من لوحة التاجر، وتنعكس فوراً على تتبع العميل).
- مصادقة كاملة: اختيار نوع الحساب، دخول/تسجيل مع تحقق من المدخلات، OTP، قفل المحاولات.
- الموقع والعناوين: إذن الموقع، نجاح/رفض، عناوين محفوظة، إضافة عنوان.
- التاجر: رحلة التوثيق (M-042 → M-049/M-048)، لوحة التحكم، الطلبات الواردة وتحديث المراحل،
  المنتجات (إضافة/تعديل/حذف مع تأكيد)، الإحصائيات.

## البنية

```
src/
  main.jsx, index.css           # نقطة الدخول + طبقات Tailwind + Cairo
  App.jsx                       # نقطة الدخول: Landing → معرض الشاشات / النموذج التفاعلي (توجيه Hash)
  gallery/catalog.js            # فهرس معرض الشاشات (102 عنصر) مع الحالة المعزولة لكل شاشة
  gallery/ComponentsShowcase.jsx# شاشات المكونات المستقلة (Nav, Header, Buttons, Fields, Cards, Tokens)
  store/AppContext.jsx          # الحالة العامة (reducer) + التنقّل + السلة + الطلبات
  data/mock.js                  # بيانات وهمية (منتجات، متاجر، عناوين، طلبات)
  components/ui.jsx             # مكوّنات مشتركة (TopBar, BottomNav, Toast, StateScreen...)
  screens/Onboarding.jsx        # Splash, Onboarding, نوع الحساب, دخول, تسجيل, OTP
  screens/Shopping.jsx          # الموقع, الرئيسية, البحث, المتاجر, المنتج, المفضلة
  screens/Cart.jsx              # السلة, الدفع, نجاح الطلب, التتبع, الطلبات, الفاتورة
  screens/Account.jsx           # حسابي (زائر/عميل/تاجر + بيانات متجري), الصفحات القانونية, المساعدة
  screens/Merchant.jsx          # رحلة التاجر + لوحة التحكم + الطلبات + المنتجات + الإحصائيات
public/img/                     # صور المنتجات والمتاجر
public/assets/logo-source.png   # الملف الرسمي المعتمد كما ورد من العميل (2033×773، خلفية شفافة) — مرجع فقط
public/assets/logo.png          # الشعار المستخدم في الواجهة = نفس الملف بعد إزالة الهوامش الشفافة الفارغة فقط (1613×447 · 3.61:1)
public/assets/logo-icon.png     # العلامة وحدها (مربعة 512²) — للأيقونات والمساحات الضيقة فقط
public/favicon.png              # أيقونة المتصفح (من العلامة)
shots/                          # لقطات شاشة من الاختبار الآلي (Playwright)
```

## الشعار الرسمي

- الشعار المعتمد هو الملف المرسل من العميل (الاسم العربي «جديد» + JADEED + العلامة البرتقالية/البنفسجية، بخلفية شفافة). الملف الأصلي محفوظ كما هو في `public/assets/logo-source.png`، والنسخة المستخدمة `public/assets/logo.png` مطابقة له بكسلاً ببكسل بعد إزالة الهوامش الشفافة الفارغة فقط (لا قصّ لأي جزء مرئي، ولا إعادة تلوين).
- الشعار السابق (المربع الأسود 120×120) ومشتقّاته حُذفت نهائياً من المشروع.
- المكوّن `Logo` في `src/components/ui.jsx` يعرضه عبر `<img>` بـ:
  - **ارتفاع ثابت** (`size` بالبكسل، أو صنف Tailwind مثل `className="h-12"`) و**عرض تلقائي** `w-auto`؛
  - `object-fit: contain` و`max-w-none` ⇒ لا تمطيط ولا قصّ مهما كانت الحاوية؛ نسبة العرض إلى الارتفاع = نسبة الصورة الأصلية دائماً (3.61:1).
  - `<Logo size={n} />` → الشعار الكامل (Splash داخل لوحة بيضاء، الدخول، التسجيل، الهيدر الموحّد، هيدر معرض الشاشات، صفحة الهبوط، دخول الإدارة).
  - `<Logo light />` → على الخلفيات البنفسجية/الداكنة (لوحة بيضاء خلف الشعار ليبقى الاسم الكحلي مقروءاً).
  - `<Logo icon />` → العلامة وحدها (مربعة) للمواضع الضيقة جداً (معاينة شعار المتجر، الأيقونات).

## ضمان الجودة (QA) — وضع النموذج التفاعلي

نُفِّذ تدقيق آلي كامل بـ Playwright على وضع النموذج (`#/prototype`) قبل الإصدار (Release Candidate) — **71/71 فحص ناجح**:

| المحور | ما تم التحقق منه |
|---|---|
| المصادقة | رفض البريد غير الصالح، قبول رقم `7xxxxxxxx`، OTP من 4 خانات، عدّاد المحاولات، القفل المؤقت |
| منطق السلة | سلة مركّبة (5 منتجات، تغيير كميات بحدّ المخزون، حذف منتج) — كل مجموع يُطابق الحساب اليدوي لحظيًا وينعكس على الدفع والفاتورة |
| دورة حياة الطلب | فشل الشبكة → شاشة الفشل الكاملة → إعادة المحاولة → نجاح → تتبّع → فاتورة؛ رفض/قبول التاجر، تجهيز، جاهز، خرج للتوصيل، تم التسليم |
| الشاشات المُستعادة ★ | «تم رفض الطلب»، «تم قبول الطلب»، «تم تعديل المنتج»، «تم حذف المنتج»، «تم اعتماد المتجر» تظهر كشاشات كاملة داخل التدفّق نفسه |
| RTL والنظافة البصرية | أرقام الطلبات `JD-XXXXXX` داخل `dir="ltr"` (لا انقلاب)، لا تداخل/تجاوز نصوص في 12 شاشة، شريط التنقّل سليم، ملاءمة 360×640 بلا تمرير أفقي، خط Cairo |
| الكونسول | لا أخطاء وقت تشغيل، لا تحذيرات React key، لا طلبات فاشلة |

كما يُفحص معرض الشاشات (106 عناصر) آليًا: لا شاشة فارغة ولا أخطاء كونسول.

سكربتات الفحص في `scripts/`: `qa-gallery.mjs` (المعرض كاملاً — 106 شاشات) · `qa-prototype.mjs` (6 تدفقات أساسية) · `qa-phase5.mjs` (36 فحصاً للمراجعة الأولى — عُدّلت في RC-8 لأن التصفح كزائر أُزيل) · `qa-phase6.mjs` (49 فحصاً للمراجعة الثانية + التوثيق الجديد) · `qa-rc8.mjs` (35 فحصاً لنقاط RC-8). آخر تشغيل (RC-8): 126/126 ناجح، صفر أخطاء كونسول.

## ملاحظات إصدار (RC-3)

- **شريط التنقّل السفلي في الرئيسية**: كان جذر شاشة الرئيسية يتمدّد بارتفاع محتواه (1820px) داخل إطار 844px فيُدفع الشريط خارج الإطار. أُصلح على مستوى الموجّه (`ScreenRouter` في `src/App.jsx`) بتقييد ارتفاع كل شاشة بإطار الهاتف (`min-h-0 max-h-full overflow-hidden`) — ينطبق على الشاشات الـ91 كلها ويُغني عن إصلاح كل شاشة على حدة.
- **أكواد Figma المرجعية (CUS-0xx / M-0xx) أُزيلت من واجهة الهاتف** (الهيدر، أزرار الإجراءات، النصوص المساعدة، رسائل Toast). تبقى الأكواد فقط في بطاقات معرض الشاشات وقائمته الجانبية خارج الإطار — لأغراض المطابقة مع التصميم — وفي `SCREENS-COVERAGE.md`.

## ملاحظات إصدار (RC-4)

- **شاشة «تفاصيل المتجر وقائمة المنتجات» (Figma 11:1464)** أُعيد بناؤها لتطابق التصميم: غلاف المتجر مع زر الرجوع، بطاقة المتجر المتداخلة (الشعار/العلامة، الاسم مع شارة التوثيق، الحي والمدينة، شارتا «معتمد ومفتوح» و«متجر رسمي موثوق»)، عنوان «قائمة منتجات المتجر» مع العدّاد، شبكة بطاقات المنتجات (صورة كبيرة + الاسم بسطرين + السعر + زر **+** يضيف فعلياً إلى السلة مع شارة الكمية)، وزر «العودة إلى الرئيسية» ثابت أسفل الشاشة. الوصول إليها من بطاقات المتاجر في الرئيسية، وقائمة «المتاجر القريبة»، ورابط المتجر في صفحة المنتج.
- **«المتجر فارغ حالياً» (CUS-020 · 11:1426)** صارت شاشة مستقلة بهيدر خاص وزر «تصفح متاجر أخرى» كما في التصميم (تظهر تلقائياً لأي متجر بلا منتجات — مثلاً بعد حذف التاجر لكل منتجاته).
- إصلاح: ملف `logo-icon.png` (العلامة المربعة للأيقونات) كان شفافاً بالكامل بسبب خطأ في القصّ؛ أُعيد توليده من الشعار الرسمي ويُستخدم الآن في بطاقة المتجر ومعاينة رفع الشعار وأيقونة المتصفح.

## ملاحظات إصدار (RC-5)

- **ظلال الأزرار**: خُفِّفت لتصبح ناعمة ومنخفضة (طبقة تلامس 1px + هالة لونية بشفافية 16–18%) مع ارتفاع طفيف عند التمرير فقط وإلغاء الظل عند الضغط. تُعرَّف في `tailwind.config.js` باسمَي `shadow-brand` (بنفسجي) و`shadow-accent` (برتقالي).
- سبب الثقل السابق: كان اسم الظلّ `shadow-primary` يتعارض مع أداة `shadow-{color}` المدمجة في Tailwind، فتُرسم الهالة بلون صلب بلا شفافية. إعادة التسمية أزالت التعارض نهائياً.
- أُضيفت سكربتات الفحص الآلي إلى `scripts/` (`qa-gallery.mjs`, `qa-prototype.mjs`) لتكون جزءاً من الحزمة.

## ملاحظات إصدار (RC-6) — المراجعة الوظيفية (10 نقاط)

| # | النقطة | الحالة قبل | ما تم |
|---|---|---|---|
| 1 | زر للمتاجر في الشريط السفلي | غير موجودة | تبويب «المتاجر» (`nearbyStores`) بدل «البحث»؛ البحث من رأس الرئيسية وأيقونة في شاشة المتاجر |
| 2 | سلة مفصولة لكل متجر | غير موجودة | `computeCart` يُرجع `groups` (منتجات + مجموع فرعي + حد أدنى لكل متجر)؛ `PLACE_ORDERS` ينشئ طلباً مستقلاً لكل متجر بأرقام متتالية |
| 3 | اسم المتجر على بطاقة المنتج | غير موجودة | `StoreLine` داخل `ProductCard` (قابل للنقر → صفحة المتجر) |
| 4 | شروط الاستخدام / الخصوصية | غير موجودة | شاشة `legal` (`doc: terms|privacy`) + روابط في حسابي (زائر ومسجّل) وفي التسجيل |
| 5 | نظام عميل/تاجر | جزئياً | `merchantStatus` يبدأ `none`؛ `LOGIN` يفعّل التاجر فقط لنوع حساب تاجر/كلاهما أو بعد اعتماد الطلب؛ `STORES` تحمل `owner/ownerAccount/phone/deliveryTime/location/payment`؛ نموذج التاجر يطلبها (خريطة بدبوس) |
| 6 | دفع التاجر + إيصال التحويل | غير موجودة | طريقة «تحويل بنكي/محفظة» تعرض حساب كل تاجر ورفع إيصال لكل متجر؛ `paymentStatus` (`pending_confirmation` → `paid`)؛ `CONFIRM_PAYMENT` ينقل الطلب إلى التجهيز تلقائياً |
| 7 | تبسيط الحالات | جزئياً | 4 مراحل: جديد → قيد التجهيز → في الطريق → تم التوصيل (+ ملغي/مرفوض)؛ انتقالان يدويان فقط؛ حُذفت شبكة أزرار المراحل |
| 8 | استعادة الحساب | غير موجودة | شاشة `forgotPassword` (قناة جوال/بريد → OTP) ورابط من الدخول |
| 9 | لا إجبار على الدخول | جزئياً (ألغي في RC-8) | ~~`requireAuth` + `returnTo`: المفضلة/الطلبات/الدفع فقط؛ التخطي والخروج → الرئيسية~~ — **RC-8: أُزيل التصفح كزائر نهائياً؛ الدخول مطلوب لكل شيء بما فيه الإضافة للسلة** كزائر؛ شاشة زائر في «حسابي» |
| 10 | صفحة المتجر | جزئياً | أُضيف: الوصف، الهاتف (`tel:`)، وقت التوصيل، صاحب المتجر، خريطة الموقع (`StoreMapPreview`) |

- فحص إضافي: `node scripts/qa-phase5.mjs` (38 فحصاً E2E للنقاط العشر).
- **حدود النموذج (بيانات وهمية):** الخريطة مصغّرة مرسومة (بلا مزوّد خرائط)، رفع الإيصال/الشعار محاكاة بلا اختيار ملف فعلي، لا حفظ دائم للبيانات (State في الذاكرة)، حالة التاجر تُعتمد بزر «محاكاة: اعتماد».

## ملاحظات إصدار (RC-7) — المراجعة الوظيفية الثانية (13 نقطة)

| # | النقطة | الحالة قبل | ما تم |
|---|---|---|---|
| 1 | حالة طلب الترقية | جزئياً | «قيد المراجعة» في حسابي وشاشة الانتظار؛ بعد الاعتماد شاشة «مقبول — المتجر نشط» بزر أساسي «الدخول إلى لوحة تحكم التاجر» |
| 2 | شاشة OTP | جزئياً | أُضيفت: حالة التحميل أثناء التحقق (0.9 ث)، عدّاد صلاحية 60 ث، حالة «انتهت صلاحية الرمز» (حقول معطّلة)، «إعادة إرسال الرمز» يعيد المؤقت ويصفّر المحاولات؛ الخطأ والقفل كانا موجودين |
| 3 | تبديل تسوق ↔ لوحة التاجر | جزئياً | زر «التسوق» في رأس لوحة التاجر + زر «لوحة تحكم المتجر» في حسابي (وضع العميل) — بلا تسجيل خروج (`merchantMode`) |
| 4 | الطلبات | جزئياً | إلغاء التاجر بعد القبول (`CANCEL_ORDER by:'merchant'` + سبب من قائمة) يظهر للعميل «ألغى المتجر هذا الطلب بعد قبوله» مع السبب وشارة «ألغاه المتجر»؛ قائمة طلبات التاجر مقسّمة: جديدة بانتظار قرارك / قيد التنفيذ / منتهية |
| 5 | المخزون | جزئياً | `ADD_TO_CART`/`SET_QTY` مقيّدان بـ `stock`؛ `Stepper` يعطّل + عند الحد ويعرض «أقصى كمية متاحة (n)» في صفحة المنتج والسلة والمتجر |
| 6 | البحث | جزئياً | حالة فارغة برسم SVG + زر «العودة إلى المنتجات المقترحة»؛ الفلاتر (السعر/المخزون/الترتيب) لوحة قابلة للطي داخل شاشة البحث نفسها (`FiltersScreen` تحوّل إليها) |
| 7 | الموقع وبيانات المستخدم | جزئياً | موقع واحد لكل حساب قابل للتحديث يدوياً (نموذج) أو من الخريطة؛ نموذج المتجر يعبّئ صاحب المتجر/الهاتف من الحساب تلقائياً |
| 8 | حالة التوفر على البطاقة | جزئياً | `AvailabilityBadge` موحّدة (متوفر / آخر n / نفد) على بطاقات الرئيسية والبحث والمتجر والمفضلة |
| 9 | المتجر | جزئياً | إيقاف الخصومات (لا `oldPrice`/كوبون/شارة) وإيقاف الإشعارات (لا جرس/شاشة)؛ زر «التسوق» داخل لوحة التاجر؛ تعديل بيانات المتجر من اللوحة ومن «حسابي». **تعدد المتاجر أُزيل لاحقاً بقرار المنتج (RC-7.1): التاجر مرتبط بمتجر واحد فقط (`MERCHANT.storeId`)** |
| 10 | بيانات العميل عند الطلب | غير موجودة | قسم «بيانات التواصل والاستلام» في إتمام الطلب (اسم، جوال، عنوان، ملاحظة) مع تحقق؛ يُحفظ في الطلب ويظهر للتاجر وفي الفاتورة |
| 11 | انقطاع الإنترنت | غير موجودة | `OfflineBanner` أعلى الشاشة (يقرأ `navigator.onLine` + زر محاكاة) مع «إعادة المحاولة» وToast عند عودة الاتصال |
| 12 | الرسائل | جزئياً | `Toast` يظهر من أسفل الشاشة فوق شريط التنقّل؛ رسائل التأكيد الحرجة تبقى نوافذ `Modal` |
| 13 | أيقونة السلة | — | `ShoppingCart` (عربة) بدل `ShoppingBag` في كل مواضع السلة |

- فحص إضافي: `node scripts/qa-phase6.mjs` (46 فحصاً E2E للنقاط الـ13).

## ملاحظات إصدار (RC-7.1) — إزالة تعدد المتاجر

بقرار المنتج، أُزيل كل ما أُضيف لدعم امتلاك التاجر أكثر من متجر؛ التاجر الآن مرتبط بمتجر واحد فقط (`MERCHANT.storeId`):

- **الحالة (`AppContext`)**: حُذفت `merchantStores` و`activeStoreId` والإجراءات `MERCHANT_ADD_STORE` / `MERCHANT_SET_ACTIVE_STORE` / `MERCHANT_REMOVE_STORE`؛ `merchantProducts` = منتجات المتجر الوحيد؛ السياق يوفّر `merchantStore` بدل `activeStore`.
- **البيانات (`mock.js`)**: حُذف `MERCHANT.storeIds`.
- **لوحة التاجر**: لا محوّل متاجر في الرأس، لا «إنشاء متجر جديد بنفس الحساب»؛ الطلبات/المنتجات/الإحصائيات بلا فلترة بمتجر نشط.
- **حسابي (وضع التاجر)**: حُذفت قائمة «متاجري» (تبديل/تعديل/حذف لكل متجر ونافذة تأكيد الحذف)؛ بقيت بطاقة «بيانات متجري» مع زر «تعديل» كما كانت.
- **نموذج المتجر**: حُذف وضع `newStore` (إضافة متجر إضافي)؛ بقي «طلب إنشاء متجر جديد» للتاجر الجديد مع التعبئة التلقائية لاسم صاحب المتجر والهاتف.
- **واجهة العميل**: أُزيلت فلاتر `store.deleted` التي أُضيفت لحذف المتاجر (لم يعد للمتجر حذف).
- ما بقي دون تغيير: التبديل تسوق ↔ لوحة التاجر، إلغاء التاجر بعد القبول، أقسام الطلبات، المخزون، البحث، الموقع الواحد، بيانات العميل عند الطلب، الانقطاع، Toast، أيقونة السلة.

## ملاحظات إصدار (RC-8) — المراجعة الوظيفية الثالثة (8 نقاط)

| # | النقطة | الحالة قبل | ما تم |
|---|---|---|---|
| 1 | نوع حساب واحد | ناقصة (كان هناك خيار «كلاهما») | حُذف `both` من `AccountType`؛ `SET_ACCOUNT_TYPE` يقبل `customer`/`merchant` فقط؛ اختصار لوحة التاجر في Demo Panel يستخدم `merchant` |
| 2 | إلغاء «تخطي/الدخول كزائر» + الإضافة للسلة تتطلب الدخول | غير موجودة (كان الزائر يتصفح ويضيف للسلة) | حُذفت أزرار «تخطي» و«تصفح كزائر» من نوع الحساب والدخول؛ `ADD_TO_CART` يرفض غير المسجّل، و`AddToCartButton` وأزرار «+» تفتح بوابة الدخول مع `returnTo`؛ الخروج يعيد إلى شاشة نوع الحساب. زر «تخطي» في شرائح التهيئة باقٍ لأنه يتخطى الشرائح إلى اختيار النوع فقط |
| 3 | قفل اسم المتجر والنشاط بعد التوثيق | غير موجودة (لم يكن للنشاط حقل أصلاً) | `MerchantStoreEdit`: حقل «نوع النشاط» جديد؛ الاسم والنشاط `disabled` مع أيقونة قفل وتنبيه عندما `merchantStatus === 'approved'`، وقابلان للتعديل قبل الاعتماد (`m-store-edit-unverified` في المعرض) |
| 4 | توثيق: صورتا بطاقة + صور واجهة | ناقصة (ملف هوية واحد) | `MerchantIdentity` أُعيدت كتابتها: وجه البطاقة + ظهرها + 2–4 صور للواجهة، شرح لكل حقل، قائمة النواقص، والمتابعة معطّلة حتى الاكتمال |
| 5 | Toggle مفتوح/مغلق بأثر حقيقي | غير موجودة | بطاقة حالة المتجر بـ `role="switch"` في اللوحة → `SET_STORE_OPEN` يغيّر `store.open`؛ لدى العميل: شارة «مغلق» في القوائم، شريط في صفحة المتجر، رفض الإضافة للسلة، شارة «مغلق حالياً» في السلة وتعطيل إتمام الطلب، وحارس في `Checkout.place()` — لا طلبات جديدة وهو مغلق (المنطق واجهة فقط — لا خادم) |
| 6 | شاشة التاجر المحظور | غير موجودة (كان وسم فقط في حسابي) | شاشة `MerchantBanned` (سبب الحظر وتاريخه من `MERCHANT`، نموذج اعتراض ≥20 حرفاً بحالة «تم الاستلام»، تسجيل خروج)؛ حارس في الراوتر يستبدل أي شاشة `m-*` بها؛ الحظر لا يُرفع بإعادة الدخول؛ بطاقة في حسابي؛ اختصار «محاكاة: متجر محظور» |
| 7 | «ضمان التاجر» | ناقصة | استُبدل النص في صفحة المنتج (الموضع الوحيد) |
| 8 | «المبلغ المستحق» | ناقصة | استُبدل «المبلغ المدفوع» في تتبع الطلب/التسليم (الموضع الوحيد؛ «حساب استلام المدفوعات» أُبقي لأنه معنى مختلف) |

شاشات أُضيفت للمعرض: `m-banned`، `account-merchant-banned`، `m-dashboard-closed`، `m-store-edit-unverified` (الإجمالي 106).
